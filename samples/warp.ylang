// ============================================================================
// warp
// ============================================================================
get := fn(p) -> p in Bounds ? @p : Transparent
warp := fn(x) -> x*pow(10, cos(x))/5
for p in Bounds {
    @p = get(p.x; (p.y + warp(p.x / 25) * 1.5))
}
